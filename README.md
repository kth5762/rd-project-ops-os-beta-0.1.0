# GPT 5.5 Pro 기본 모델 변경

이 패키지는 `ChatGPT 업무지원` 탭의 기본 모델을 `gpt-5.5-pro`로 설정한 GitHub Pages 교체 파일입니다.

## 교체 대상

- `index.html`
- `app-config.js`
- `assets/`
- `google-apps-script/Code.gs`

Apps Script Script Properties의 `OPENAI_MODEL`도 `gpt-5.5-pro`로 설정하면 프론트와 서버 기본값이 동일하게 유지됩니다.
